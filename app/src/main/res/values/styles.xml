<resources>

    <style name="BaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:textColorPrimary">@color/primaryText</item>
        <item name="android:textColorSecondary">@color/secondaryText</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="themedCardBackgroundColor">@color/itemPrimaryBackgroud</item>
        <item name="themedBackgroundColor">@color/primaryBackgroud</item>
        <item name="android:divider">@color/divider</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="BaseTheme">
        <!-- Customize your theme here. -->
    </style>

    <!-- Base application theme. -->
    <style name="AppThemeMain" parent="AppTheme">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="BaseTheme.Dark" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/colorPrimaryInverse</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarkInverse</item>
        <item name="colorAccent">@color/colorAccentInverse</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:textColorPrimary">@color/primaryTextInverse</item>
        <item name="android:textColorSecondary">@color/secondaryTextInverse</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="themedCardBackgroundColor">@color/itemPrimaryBackgroudInverse</item>
        <item name="themedBackgroundColor">@color/primaryBackgroudInverse</item>
        <item name="android:divider">@color/dividerInverse</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme.Dark" parent="BaseTheme.Dark">
        <!-- Customize your theme here. -->
    </style>

    <!-- Base application theme. -->
    <style name="AppThemeMain.Dark" parent="AppTheme.Dark">
        <!-- Customize your theme here. -->
    </style>
</resources>
